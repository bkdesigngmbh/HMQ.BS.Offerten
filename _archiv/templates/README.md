# Archivierte Word-Templates

Archiviert am 2026-07-30 aus `hmq-offerten/public/`, weil nur V13 im Einsatz ist und die
alten Fassungen bei jedem Deployment mit ausgeliefert wurden.

| Datei | Stand |
|---|---|
| `Offerte_Template_V11.docx` | vor Vergleichsaufnahme |
| `Offerte_Template_V12.docx` | vor EMG, Build-Quelle von `scripts/build_template_v13.py` |

V12 bleibt hier liegen, damit der Build von V13 reproduzierbar bleibt; das Script liest die
Quelle aus diesem Ordner. Nicht in Word öffnen und speichern (zerstört die Platzhalter,
siehe `hmq-offerten/CLAUDE.md`).
